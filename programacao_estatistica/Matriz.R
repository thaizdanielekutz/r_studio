#MATRIZ
# Substitua "valores a preencher a matriz" pelos valores que você deseja preencher na matriz
# Substitua 'nrow' pelo número desejado de linhas
# Substitua 'ncol' pelo número desejado de colunas

matriz <- matrix(data = c("valores a preencher a matriz"), nrow = 2, ncol = 3)

# Exemplo com valores concretos:
matriz_exemplo <- matrix(data = c(1, 2, 3, 4, 5, 6), nrow = 2, ncol = 3)

# Exibindo a matriz
print(matriz_exemplo)